/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'aceternity.com'],  // Add all necessary external domains
    },
};

export default nextConfig;
