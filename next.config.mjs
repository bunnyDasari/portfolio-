/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'aceternity.com'],  // Add all necessary external domains
    },
    images: {
        domains: ['encrypted-tbn0.gstatic.com', 'c8.alamy.com', 'themewagon.com'],
      },


};

export default nextConfig;
